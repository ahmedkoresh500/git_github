<?php
/*
    * about tags and releases:
    
    * [git tag]                         =>> to list all tags  =>> two are the same
    * [git tag -l]                      =>> to list all tags  =>> two are the same
    
    * git tag -l "v1.0"                 =>> to list specific tag
    * git tag -l "v1.*"                 =>> to list all tags [start with] [v1.]
    
    * [git tag -d v1.0]                 =>> to delete specific tag [local repo]  =>> two are the same
    * [git tag --delete "v1.0"]         =>> to delete specific tag [local repo]  =>> two are the same
    
    * [git push origin -d v2.0]         =>> to delete specific tag [on github]  =>> two are the same
    * [git push origin --delete v2.0]   =>> to delete specific tag [on github]  =>> two are the same
*/
?>